<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Kickball Teams</title>
  <link href="styles.css" media="screen" rel="stylesheet"/>
</head>
<body>

  <h1 class="title"><%= @player[:team] %></h1>

  <ol>
      <% @players.each do |x| %>
        <% if x[:team] == @player[:team] %>
            <li><%= "#{x[:first_name]} #{x[:last_name]} | #{x[:position]}" %></li>
        <% end %>
      <% end %>
  </ol>

</body>
</html>


